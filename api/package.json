{
  "name": "restaurant-api",
  "version": "1.0.0",
  "private": true,
  "description": "json-server mock API for Assignment 6",
  "scripts": {
    "start": "json-server --watch db.json --config server.json",
    "start:pretty": "json-server --watch db.json --config server.json --middlewares ./pretty-log.js"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
